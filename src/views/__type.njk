{% macro type(fileMap, type, basepath) -%}
  {%- if fileMap[type] -%}
    <a href="{{ basepath }}/{{ fileMap[type].relative }}">{{ type }}</a>
  {%- else -%}
    {{ type }}
  {%- endif -%}
{%- endmacro %}
